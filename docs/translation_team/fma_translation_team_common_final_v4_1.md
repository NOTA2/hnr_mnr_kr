# 강철의 연금술사 미주의 윤무곡 한국어 번역팀 공통 지침 v4.1

> 이 문서는 번역 에이전트와 검수 에이전트가 반드시 함께 참고해야 하는 공통 기준이다.  
> 역할별 작업 방식은 `fma_translation_agent_final_v4_1.md`, `fma_review_agent_final_v4_1.md`를 따른다.

---

## 0. 최우선 원칙

1. 원문에 없는 설정, 감정, 복선, 설명을 임의로 추가하지 않는다.
2. 작품 고유 용어와 인명 표기는 이 문서의 용어표를 우선한다.
3. 캐릭터 대사, 시스템 메시지, 기술 설명, 아이템 설명은 서로 다른 문체로 번역한다.
4. 한국어 플레이어가 실제 GBA RPG를 플레이할 때 자연스럽게 읽히는 문장을 목표로 한다.
5. 고정 슬롯/미확장 UI 계열은 byte/문자 한계 안에서 명확하게 번역하되, 단순히 짧게 줄이는 것이 목표는 아니다. 허용된 길이를 가능한 한 활용해 의미, 말투, 조사, 목적어를 보존한다. 확장 패킹 또는 direct/structural repoint 가 가능한 항목은 원문 슬롯 길이에 억지로 맞추지 말고 자연스러운 한국어를 우선한다.
6. 추출 JSON에 기존 `translation` 값이 있어도 기준으로 삼지 않는다. 임시 번역은 무시하고, 이 지침을 우선한다.
7. 깨진 문자열, 의미 없는 바이트 추출, 문자가 아닌 데이터로 보이는 값은 추측 번역하지 않는다.
8. 정발/공식 용례가 있는 원작 고유명사는 원작 용례를 우선하고, 게임 오리지널 고유명사는 제공된 『미주의 윤무곡』 PDF의 표기를 우선한다.
9. 사람이 `manual_locked=true` 로 잠근 항목의 `translation` 은 번역 에이전트가 덮어쓰면 안 된다.
10. 수동 잠금 항목에서 더 나은 안이 있더라도, 에이전트는 `agent_draft` 와 `agent_comment` 로만 제안해야 한다.
11. 일본어/한자 자체를 제외한 원문의 특수기호, 문장부호, 번호 기호는 가능한 한 원문 형태를 유지한다. 단, 반각/전각 정책은 source family 별로 다르게 적용한다. `Registry D`, 코어 UI, 게임 용어 계열에서 이미 반각 공백/숫자/기호가 안정적으로 쓰인 항목은 반각을 유지한다. `Entry8`, 이벤트 연출 텍스트, 오프닝/세이브/선택지 같은 counted/fixed 계열은 전각을 기본으로 한다. 인명/고유명사 내부의 일본식 중점 `・`는 한국어 번역에서 띄어쓰기로 바꾸고, `・・・` 말줄임표나 `１・２` 같은 숫자 구분 기호는 원문 기호를 유지한다.

---

## 0.5 레이아웃 제약과 제어문자 규칙

현재 프로젝트에서 **객관적으로 확정된 것**과 **아직 미확정인 것**을 구분한다.

### 확정된 것

- 일부 텍스트는 일반 `00` 종단 문자열이 아니라, **고정 길이 슬롯** 안에 바로 들어간다.
- 대표 예시는 시작 화면 첫 카드 `4`줄이다.
  - 1줄: `6 bytes`
  - 2줄: `16 bytes`
  - 3줄: `18 bytes`
  - 4줄: `22 bytes`
- 이런 고정 길이 슬롯은:
  - 자동 줄바꿈이 없다.
  - 번역문 뒤에 `00 terminator`를 붙이면 안 된다.
  - 바로 뒤 제어코드를 밀어버릴 수 있으므로 **정확한 byte_length 안에 들어가야 한다.**
- 고정 길이 슬롯에서는 번역자가 임의로 `\n` 같은 강제 줄바꿈을 넣지 않는다.
- 현재 한글 폰트는 `가~힣` 완성형 기준으로 들어가며, 시작 화면에서는 한글 본체/그림자도 정상 매핑되도록 맞춘 상태다.
- 숫자와 기본 기호는 현재 기준으로 원본 게임 폰트를 재사용한다.
- 반각 사용은 source family 별로 다르게 본다. `registry_d_fc_script_texts` 는 반각 사용과 packed relocation 이 가능하다. 코어 UI/게임 용어 계열은 현재 번역 데이터에서 반각 공백/숫자/기호가 광범위하게 쓰이고 있으므로 기존 반각을 보존한다. `registry_a_entry8_prefixed_texts`, `inline_event_texts`, `startup_intro_texts`, `save_menu_texts`, `choice_yes_no_texts` 는 전각 우선 계열로 둔다.
- 영문 약어/버튼 표기는 반각 ASCII 가 아니라 **전각 영문**을 사용한다. 반각 `HP`, `AS`, `R 버튼` 은 화면에서 일부 글자가 누락될 수 있으므로 `ＨＰ`, `ＡＳ`, `Ｒ 버튼` 으로 쓴다.

### 부분적으로 구조가 확정된 것

- save/menu 일부 텍스트는 일반 `00` 종단 문자열이 아니라 **`01 FF <u16 char_count>` command-stream 레코드** 다.
- 현재 대표 예시는 [save_menu_prefixed_texts.json](/Users/user/test/confirmed_data/extracted_texts/save_menu_prefixed_texts.json) 이다.
- 이 family 에서는:
  - `char_count` 헤더를 보존/갱신해야 한다.
  - 본문 뒤에 `00 terminator`를 임의로 붙이면 안 된다.
  - 본문 바로 뒤의 `10 FF`, `04 FF`, `14 FF`, `08 FF` 같은 제어코드를 밀면 안 된다.
  - 원문에 없는 임의 줄바꿈을 넣지 않는다.
- 세부 구조는 [text_box_family_manifest.json](/Users/user/test/confirmed_data/text_layout/text_box_family_manifest.json) 의 `save_menu_prefixed_01ff` family 를 따른다.
- Registry A entry `8` 대형 대사/이벤트 bank 도 일반 `00` 종단 문자열이 아니라 **`01 FF <u16 char_count>` counted script line** 구조다.
- 이 family 에서는:
  - `char_count` 헤더를 보존/갱신해야 한다.
  - 본문 뒤에 `00 terminator`를 임의로 붙이면 안 된다.
  - 본문 바로 앞뒤의 script control bytes 를 밀면 안 된다.
  - 원문에 없는 임의 줄바꿈을 넣지 않는다.
- 세부 구조는 같은 manifest 의 `entry8_prefixed_01ff_script_line` family 를 따른다.
- 현재 관측상 이 source 는 **explicit newline 없는 short single-line payload profile** 로 보는 편이 가장 안전하다.
- Entry8 fixed/in-place 계열은 “짧게”가 아니라 **허용 문자수 안에서 최대한 의미를 살리는 것**을 목표로 한다.
  - Entry8 fixed/in-place 계열의 실전 제한은 `max_korean_chars` / `char_count` 를 기준으로 본다. 단순 UTF-8 byte 길이로 과도하게 줄이지 않는다.
  - 허용 문자수 안에 들어간다면 전각 공백 `　`을 적극적으로 써서 자연스러운 한국어 띄어쓰기를 살린다. 예: `솔링사건도아닌가` 는 11자 제한 안에서 `솔링　사건도　아닌가`처럼 고친다.
  - 원문에 있는 문장부호와 특수기호는 띄어쓰기 목적 외에는 임의로 제거하지 않는다. 용량이 허락하면 원문의 `、`, `。`, `！`, `？`, `…`, 괄호, 따옴표 계열을 보존한다.
  - 다만 문장부호 바로 뒤에 불필요한 공백은 넣지 않는다. 문장부호 자체가 화면에서 충분한 간격을 만들기 때문이다.
  - 짧게 줄이는 것은 마지막 수단이다. 먼저 `max_korean_chars`를 최대한 활용해 의미, 말투, 띄어쓰기, 원문 특수기호를 살린다.
  - 사용 가능 길이가 충분한데도 의미를 5자 안팎으로 과도하게 압축하지 않는다.
  - 조사/목적어/화자의 말투를 살릴 수 있으면 허용 길이 안에서 살린다.
  - 의미 없는 padding 이나 원문에 없는 설명으로 길이를 채우지는 않는다.
  - `confirmed_repoint` 처럼 길이 변경이 가능한 Entry8 레코드는 길이를 크게 의식하지 말고 자연스러운 의미 보존을 우선한다.
- Registry D 이벤트/튜토리얼 대사는 일반 `00` 종단 문자열이 아니라 **`FC stop-byte` script line** 구조다.
- 이 family 에서는:
  - record boundary 를 `FC` 기준으로 유지해야 한다.
  - 본문 뒤에 `00 terminator`를 임의로 붙이면 안 된다.
  - 원문에 있던 명시적 개행은 보존 우선으로 본다.
  - `before_bytes / after_bytes` 로 대표되는 script control stream 을 밀지 않는다.
- 현재 삽입기는 Registry D entry 를 ROM 끝으로 다시 패킹하고 `0x17C7E4` pointer-length table 을 갱신할 수 있다.
- 따라서 Registry D 대사는 원문 byte 슬롯보다 길어질 수 있으며, 번역자는 의미를 과도하게 압축하기보다 자연스러운 문장과 화면 줄폭을 우선한다.
- 세부 구조는 같은 manifest 의 `registry_d_fc_stop_script_line` family 를 따른다.
- 현재 관측상 이 source 는 **explicit multiline payload profile** 이므로, 원문 개행 보존이 특히 중요하다.
- system message 일부는 일반 `00` 종단 문자열이지만, 이미 원문 안에 **명시적 개행(`\n`)** 이 들어 있다.
- 이 family 에서는 원문에 들어 있던 개행 수와 위치를 함부로 바꾸지 않는다.
- 세부 구조는 같은 manifest 의 `system_messages_plain_newline_00` family 를 따른다.
- `ui_skill`, `item`, `Registry A entry 12` 일부 gameplay term 은 **`00` 종단 plain record** family 로 부분 확정됐다.
- 이 family 에서는:
  - 번역문 뒤에 `00 terminator`는 공용 삽입기가 붙인다.
  - 원문에 없는 임의 줄바꿈을 넣지 않는다.
  - 직접 포인터가 확인된 항목은 슬롯을 넘겨도 repoint 가능하므로 자연스러운 표현을 우선한다.
  - 직접 포인터가 없는 항목은 원래 슬롯 안에서 가장 의미가 분명한 표현을 고른다.
- 세부 구조는 같은 manifest 의 `ui_or_item_plain_00_record` family 를 따른다.
- `battle`, `ability`, `material` 일부는 **`00` 종단 plain record + optional inline 0x0B separator** family 로 부분 확정됐다.
- 이 family 에서는:
  - embedded `0x0B` separator 를 함부로 지우거나 바꾸지 않는다.
  - `0x0B` 는 작업 데이터와 ROM 런타임 양쪽의 이름/설명 구분자이며, 삽입/정규화 도구는 원문 이름 필드 폭을 기준으로 앞쪽 padding 을 자동 복원하고 `0x0B` 자체도 유지한다.
  - 번역자는 전각/반각 공백을 직접 넣어 이름 필드를 맞추지 않는다.
  - 원문 이름 필드 폭보다 한국어 앞줄이 짧아 여백이 생기면, 가능하면 그 여백을 의미 있는 조사/목적어/동사 조각으로 활용한다. 예: `오토메일검을\x0b연성해 공격`, `벽을 연성해\x0b상대 공격`, `강철 대포를\x0b연성해 공격`.
  - 2줄 설명창에서는 윗줄을 먼저 채운다. byte 와 첫 줄 표시 폭이 허용하면 `창을\x0b연성해 공격` 보다 `창을 연성해\x0b공격`, `해머를\x0b연성해 공격` 보다 `해머를 연성해\x0b공격` 처럼 배치한다.
  - 앞줄 여백 활용은 실제 글자를 추가하는 정책이지 수동 padding 을 넣는 정책이 아니다. 최종 정렬 공백은 정규화/빌드 도구가 복원한다.
  - 원문에 없는 임의 줄바꿈은 기본적으로 넣지 않는다. 단, 시각적으로 2줄 설명창 계열임이 확인된 짧은 `ability_texts` 설명에서 한 줄 배치가 잘리거나 답답하면, byte 한계와 첫 줄 폭 확인 후 `0x0B` 를 넣을 수 있다.
  - 포인터가 없는 고정 슬롯이 많으므로 `battle_texts`, `ability_texts`, `material_texts` 번역은 audit 의 `expansion_mode` 를 확인한다. 슬롯 여유가 있으면 더 알아보기 쉬운 표현을 쓰고, 여유가 없으면 byte 한계를 지킨다.
- 세부 구조는 같은 manifest 의 `term_description_plain_00_optional_0b` family 를 따른다.
- credits 문자열은 **전각 공백 패딩을 가진 `00` 종단 plain record** family 로 부분 확정됐다.
- 이 family 에서는 padding 이 레이아웃 힌트 역할을 하므로, spacing-sensitive 수정은 매우 보수적으로 한다.
- 세부 구조는 같은 manifest 의 `credits_padded_plain_00_record` family 를 따른다.

### 아직 전역 확정되지 않은 것

- 일반 대사창의 `한 줄 최대 글자 수`
- 일반 대사창의 `한 박스 최대 줄 수`
- 화면군별 자동 줄바꿈 규칙
- `\n`, `[WAIT]`, `[PAGE]`, `{NAME}` 같은 제어 토큰의 실제 사용 범위
- `…`, `―`, `!`, `?`, `・`, `《》` 같은 문장부호의 전체 화면 호환성

### 현재 번역팀 운영 규칙

- 전역 수치가 확정되기 전까지는 “한 줄 14자” 같은 값을 임의로 상정하지 않는다.
- 원문에 명시된 줄바꿈/제어문자가 있으면 **보존 우선**으로 본다.
- 줄바꿈 정보가 없는 일반 대사는 source family 별 삽입 방식을 먼저 본다. Registry D처럼 확장 패킹이 가능하거나 plain record 직접 포인터가 확인된 항목은 자연스러운 한국어를 우선하고, 아직 확장 경로가 없는 counted/UI 계열은 byte 한계 안에서 명확하게 유지한다.
- 길이 제약이 확정된 workset 은 별도 메모나 필드로 관리하고, 그 규칙이 있으면 그 규칙을 최우선한다.
- 화자 정보가 필요한 경우, 현재는 extracted JSON 안의 명시적 `speaker` 필드 대신 [confirmed_data/dialogue_metadata/README.md](/Users/user/test/confirmed_data/dialogue_metadata/README.md) 의 `dialogue_state_token` sidecar 를 먼저 참고한다.
- `dialogue_state_token` 은 확정 화자명이 아니라 **동일 portrait/state 후보를 묶는 객관적 제어 표식**으로만 취급한다.
- `entry8` 와 `Registry D` 는 둘 다 대사 source 지만 runtime profile 이 다르므로, 둘에 같은 줄바꿈 습관을 적용하지 않는다.
- [runtime_dialogue_subprofiles.md](/Users/user/test/confirmed_data/text_layout/runtime_dialogue_subprofiles.md) 기준으로:
  - `entry8` 의 chain-heavy cluster 는 짧은 single-line record 가 외부 script control 로 연속 표시될 가능성이 높으므로, 각 record 를 한 줄 단위로 명확하게 번역한다. 단, fixed 계열이라도 단순 최소 길이로 줄이지 말고, 허용 문자수 안에서 의미와 말투를 최대한 살린다.
  - `Registry D` 의 long-multiline tier 는 확장 패킹으로 원문 byte 슬롯보다 길게 넣을 수 있으므로, 의미를 덜어내기보다 원문 개행 보존과 시각 QA 를 우선한다.

### 코드 기반으로 강하게 좁혀진 것

아래 값은 **실행 화면 전수 확인까지 끝난 하드 제한**은 아니지만, 현재 코드 분석상 매우 강한 후보다.
세부 근거는 [text_box_family_manifest.json](/Users/user/test/confirmed_data/text_layout/text_box_family_manifest.json) 을 따른다.
workset/source 연결은 [text_layout_assignment_index.json](/Users/user/test/confirmed_data/text_layout/text_layout_assignment_index.json) 을 따른다.
- high-priority dialogue profile 차이는 [runtime_dialogue_family_report.md](/Users/user/test/confirmed_data/text_layout/runtime_dialogue_family_report.md) 도 함께 본다.

- 공용 text object family 다수 caller (`0x062182`, `0x065AA4`, `0x06718A` 등) 는 모두 `r3=20` 으로 `0x014A98` 를 호출한다.
- 이 family 는 내부적으로 capacity `30` mixed-width units 로 변환된다.
- 코드상 현재 가장 강한 해석은:
  - fullwidth 문자 약 `20`자
  - halfwidth 문자 약 `30`자
- 월드맵 지역명 family (`0x06A972`) 는 `r3=12` 를 사용하고, capacity `18` units 로 변환된다.
- 코드상 현재 가장 강한 해석은:
  - fullwidth 문자 약 `12`자
  - halfwidth 문자 약 `18`자

주의:

- 위 수치는 번역 길이 감을 잡는 참고치다.
- 아직 일반 대사창 전 화면군에서 시각 QA까지 끝난 것은 아니므로, 번역팀은 이 값을 **절대 제한**처럼 단정하지 않는다. Registry D 는 자연스러운 번역을 우선하고, 화면에서 잘리면 줄바꿈/문장 분할로 조정한다.

---

## 1. 작품 배경 요약

『강철의 연금술사 미주의 윤무곡』은 GBA용 RPG이며, 2003년판 애니메이션 계열의 설정과 분위기를 기반으로 한 외전성 작품이다.

- 무대는 연금술이 과학 체계로 성립한 군사국가 아메스트리스다.
- 연금술은 물질을 이해하고 분해·재구성하는 기술이며, 기본 원리는 등가교환이다.
- 에드워드 엘릭과 알폰스 엘릭은 과거 인체연성의 실패로 잃은 몸을 되찾기 위해 여행한다.
- 에드워드는 국가 연금술사이며, 별칭은 강철의 연금술사다.
- 알폰스는 영혼이 갑옷에 정착한 상태다.
- 이 게임에서는 키메라 사건, 소재와 기술 연성, 링커 및 게임 오리지널 인물 관련 사건이 중심이 된다.

배경지식은 번역을 자연스럽게 하기 위한 보조 정보다. 원문에 없는 배경 설명을 대사 안에 추가해서는 안 된다.

---

## 2. 고유명사와 띄어쓰기 원칙

### 2.1 기본 원칙

- 고유명사는 한국어 자연 띄어쓰기를 적용한다.
- 인명은 성과 이름을 띄어 쓴다.
- 지명 + 일반 명사도 띄어 쓴다.
- 계급과 직책은 인명 뒤에서 띄어 쓴다.
- 단, 이미 하나의 고정 명칭으로 굳은 용어는 용어표를 따른다.

예:

- `エドワード・エルリック` → `에드워드 엘릭`
- `アルフォンス・エルリック` → `알폰스 엘릭`
- `マスタング大佐` → `머스탱 대령`
- `ブラッドレイ大総統` → `브래드레이 대총통`
- `オルヘンティノス城` → `올헨티노스 성`

비권장:

- `에드워드엘릭`
- `알폰스엘릭`
- `머스탱대령`
- `브래드레이대총통`
- `올헨티노스성`

### 2.2 계급/직책 표기

| 원문 | 번역 |
|---|---|
| 大総統 | 대총통 |
| 大佐 | 대령 |
| 中佐 | 중령 |
| 少佐 | 소령 |
| 中尉 | 중위 |
| 少尉 | 소위 |
| 教主 | 교주 |
| 司令部 | 사령부 |

계급은 붙이지 않는다.

- 권장: `머스탱 대령`, `마틴스 중령`, `브래드레이 대총통`
- 비권장: `머스탱대령`, `마틴스중령`, `브래드레이대총통`

---

## 3. 원작 캐릭터 고유명사

원작에 등장하는 인물은 원작/정발 계열에서 널리 쓰이는 한국어 표기를 우선한다.

| 원문 | 권장 표기 | 비고 |
|---|---|---|
| エドワード・エルリック | 에드워드 엘릭 | 애칭: 에드 |
| エド | 에드 | |
| アルフォンス・エルリック | 알폰스 엘릭 | 애칭: 알 |
| アル | 알 | |
| ロイ・マスタング | 로이 머스탱 | 대사에서는 `머스탱 대령` 가능 |
| マスタング大佐 | 머스탱 대령 | 계급 띄어쓰기 |
| リザ・ホークアイ | 리자 호크아이 | |
| ホークアイ中尉 | 호크아이 중위 | |
| アレックス・ルイ・アームストロング | 알렉스 루이 암스트롱 | |
| アームストロング少佐 | 암스트롱 소령 | |
| キング・ブラッドレイ | 킹 브래드레이 | |
| ブラッドレイ大総統 | 브래드레이 대총통 | `브래들리`보다 `브래드레이` 우선 |
| ウィンリィ・ロックベル | 윈리 록벨 | |
| ロゼ・トーマス | 로제 토마스 | |
| コーネロ | 코넬로 | |
| スカー | 스카 | |
| ラスト | 러스트 | |
| グラトニー | 글러트니 | |
| ジャン・ハボック | 쟝 하보크 | |
| ハボック | 하보크 | |
| シェスカ | 셰스카 | |
| エリシア | 엘리시아 | |
| ブラックハヤテ号 | 블랙 하야테 호 | |

---

## 4. 게임 오리지널 고유명사

게임 오리지널 인물/지명/사건명은 제공된 『미주의 윤무곡』 PDF 표기를 우선한다.

### 4.1 주요 인물

| 원문/후보 | 권장 표기 | 비고 |
|---|---|---|
| コーニッシュ・ロイス / コニシュ・ロイス 계열 | 코니슈 로이스 | 히로인 |
| コニィ | 코니 | 코니슈의 애칭 |
| セラフィ・ロイス | 세라피 로이스 | 코니슈의 오빠 |
| ロイス大佐 | 로이스 대령 | 세라피 로이스를 가리키는 군 계급 호칭 |
| リンカー | 링커 | 최종보스의 이명/호칭 |
| ラストリンカー | 라스트 링커 | 최종 형태 |
| アストン・マーティンス | 아스턴 마틴스 | 계급: 중령 |
| マーティンス中佐 | 마틴스 중령 | 대사에서는 성+계급 우선 |
| ケイト・ラム | 케이트 람 | |
| ランディ・ローバー | 랜디 로버 | |
| ダート・ダイムラー | 다트 다이무라 | PDF 기준 |
| ダイムラー | 다이무라 | `다임러` 금지 |
| バルド | 발드 | 적의 단 두목 |

### 4.2 적/키메라/사건 관련

| 원문/후보 | 권장 표기 | 비고 |
|---|---|---|
| 赤の団 | 적의 단 | PDF 기준 |
| ゴリアテ 계열 | 골리앗 | 코니슈 시험 상대 |
| ライノスロス 계열 | 라이노슬로스 | 다이무라 저택 키메라 |
| ヒドラ | 히드라 | 군 사령부/연병장 키메라 |
| ブリストル | 브리스톨 | 올헨티노스 성의 거대 키메라 |
| 黒色のエド / 黒いエド 계열 | 흑색의 에드 | 에필로그 가짜 에드 문맥 |

### 4.3 장소명

| 원문 | 권장 표기 | 비고 |
|---|---|---|
| セントラルシティ | 센트럴 시티 | 원작/게임 공통 |
| 中央司令部 | 중앙사령부 | 문맥에 따라 `센트럴 중앙사령부` 가능 |
| 東方軍司令部 | 동방군 사령부 | |
| イーストシティ | 이스트 시티 | |
| リオール | 리올 | |
| ヴィヴァス | 비바스 | |
| レドンド | 레돈드 | PDF 기준 |
| ソリン | 솔링 | |
| リゼンブール | 리젠블 | 원작 지명 |
| クルス遺跡 | 크루스 유적 | |
| ダイムラー邸 | 다이무라 저택 | `다임러 저택` 금지 |
| オルヘンティノス城 | 올헨티노스 성 | |

---

## 5. 핵심 용어표

### 5.1 세계관·작품 용어

| 원문 | 번역 | 비고 |
|---|---|---|
| 錬金術 | 연금술 | 고정 |
| 錬金術師 | 연금술사 | 고정 |
| 国家錬金術師 | 국가 연금술사 | 띄어쓰기 적용 |
| 錬成 | 연성 | 고정 |
| 等価交換 | 등가교환 | 붙여 씀 |
| 人体錬成 | 인체연성 | 붙여 씀 |
| 治療錬成 | 치료연성 | 붙여 씀. 대사/설명문에서 `치료 연성`도 문맥상 허용 |
| 治癒錬成 | 치유 연성 | 기술명으로 등장할 때 우선. `治療錬成`은 세계관/스토리 용어, `治癒錬成`은 기술명으로 구분 |
| 生体錬成 | 생체연성 | 붙여 씀 |
| 錬成融合 | 연성융합 | 붙여 씀 |
| 賢者の石 | 현자의 돌 | 고정 |
| 機械鎧 / オートメイル | 오토메일 | `기계갑옷`, `기계 갑옷` 금지 |
| キメラ | 키메라 | 고정 |
| 合成獣 | 키메라 | `합성수` 사용하지 않음 |
| ホムンクルス | 호문쿨루스 | 고정 |
| 奇跡の業 | 기적의 업 | 리올/코넬로 종교 문맥의 고정 표현 |
| 大衆 | 대중 | 핵심 문구에서는 반드시 `대중` |
| 軍部 | 군부 | |
| イシュヴァール | 이슈발 | |

### 5.2 게임 시스템 용어

추출 튜토리얼과 PDF의 시스템 설명을 함께 반영한다. 게임 내부 튜토리얼에서 `質量 / 価値 / レベル`이 확인되므로, 실제 튜토리얼 번역에서는 `질량 / 가치 / 레벨`을 우선한다. PDF의 시스템 설명에는 카드 구조가 `속성 / 레벨 / 넘버`로 설명되므로, 카드 조합 설명에서는 `속성 / 레벨 / 넘버`도 사용한다.

| 원문 | 번역 | 비고 |
|---|---|---|
| 素材 | 소재 | `자재`보다 우선 |
| 素材錬成 | 소재 연성 | 띄어쓰기 적용 |
| 技 | 기술 | 메뉴/설명 공통 |
| 技錬成 | 기술 연성 | |
| カード | 카드 | |
| 属性 | 속성 | PDF 카드 구조 기준 |
| 質量 | 질량 | 튜토리얼 추출 기준 |
| 価値 | 가치 | 튜토리얼 추출 기준 |
| ナンバー / 番号 | 넘버 | 카드 구조 문맥 |
| レベル | 레벨 | UI에서는 `Lv` 허용 |
| ベース | 베이스 | 첫 선택 소재 설명에 사용 |
| コマンド | 커맨드 | `명령어`보다 우선 |
| ルール | 규칙 | 튜토리얼 문맥에서는 `규칙` |
| 上限 | 상한 | |
| 回復 | 회복 | |
| 体力 | 체력 | 원문이 `体力`일 때 |
| HP / ＨＰ | ＨＰ | 원문이 HP일 때. 반각 `HP` 금지 |
| コンビネーションアタック | 콤비네이션 어택 | |
| アクションゲージ | 액션 게이지 | |

### 5.3 카드 속성

| 원문 | 번역 |
|---|---|
| 水 | 물 |
| 石 | 돌 |
| 鉄 | 철 |
| 自然 | 자연 |
| 暗黒 | 암흑 |

### 5.4 시스템·메뉴 용어

| 원문 | 번역 |
|---|---|
| セーブ | 저장 |
| ロード | 불러오기 |
| 記録 | 기록 |
| 通信 | 통신 |
| 通信エラー | 통신 오류 |
| ケーブル | 케이블 |
| データ | 데이터 |
| 上書き | 덮어쓰기 |
| 終了 | 종료 |
| キャンセル | 취소 |
| はい | 예 |
| いいえ | 아니요 |
| 戻る | 돌아가기 |
| 使う | 사용하기 |

---

## 6. 핵심 고정 문구

다음 문구는 작품 주제와 연결되는 핵심 문장이므로 매번 동일하게 번역한다.

| 원문 | 고정 번역 |
|---|---|
| 錬金術師よ、大衆のためにあれ | 연금술사여, 대중을 위해 존재하라 |

비권장:

- 연금술사여, 민중을 위하여 있어라
- 연금술사는 대중을 위해 있어야 한다
- 연금술사여, 사람들을 위해 살아라

---

## 7. 시스템 메시지 문체 규칙

시스템 메시지는 캐릭터 대사가 아니라 게임 UI가 플레이어에게 전달하는 문장이다. 기본적으로 존댓말을 사용한다.

### 7.1 불가 / 오류 / 완료 / 결과 알림

공식적인 시스템 알림은 `~습니다` 체를 사용한다.

| 원문 | 권장 |
|---|---|
| セーブはできないぞ | 저장할 수 없습니다. |
| データ転送できませんでした | 데이터를 전송하지 못했습니다. |
| セーブしました | 저장했습니다. |
| アイテムが足りません | 아이템이 부족합니다. |

비권장:

- 저장할 수 없다
- 저장 못 해요
- 저장은 못 한다고!

### 7.2 확인 / 선택 질문

플레이어에게 선택을 묻는 문장은 `~까요?`를 우선 사용한다.

| 원문 | 권장 |
|---|---|
| 今までの旅をセーブするか？ | 지금까지의 여정을 저장할까요? |
| これまでの旅を記録する？ | 지금까지의 여정을 기록할까요? |
| 前のデータに上書きするぞ？ | 이전 데이터에 덮어쓸까요? |
| このまま旅を続けるか？ | 이대로 여행을 계속할까요? |

`~하시겠습니까?`는 길고 딱딱하므로 기본적으로 피한다. 단, 중요한 최종 확인이나 경고성 선택지에서는 허용한다.

### 7.3 진행 중 / 대기 / 상태 표시

진행 상태는 문장형보다 짧은 명사형을 우선한다.

| 원문 | 권장 |
|---|---|
| セーブ中だ… | 저장 중... |
| 通信中・・・ | 통신 중... |
| ロード中 | 불러오는 중... |

### 7.4 안내문

안내문은 `~주세요`, `~마세요`를 사용한다.

| 원문 | 권장 |
|---|---|
| ケーブルを確認してください | 케이블을 확인해 주세요 |
| 接続数を１台にしてください | 접속 수를 1대로 맞춰 주세요 |
| 電源を切らないでください | 전원을 끄지 마세요 |

### 7.5 시스템 메시지 마침표 기준

- 단독 문장형 알림은 마침표를 붙인다.
  - 예: `저장할 수 없습니다.`
- 2줄 이상의 짧은 UI 알림은 마침표를 생략할 수 있다.
  - 예: `통신 오류!\n데이터를 전송하지 못했습니다`
- 진행 상태에는 마침표를 붙이지 않는다.
  - 예: `저장 중...`

---

## 8. 캐릭터별 말투 기준

캐릭터 말투 예시는 정발 대사를 직접 인용한 것이 아니라, 정발/원작 용어와 캐릭터 성격, 제공된 게임 자료를 참고해 만든 프로젝트용 샘플이다.

### 8.1 에드워드 엘릭

- 기본 반말.
- 짧고 직설적.
- 자신감 있고 약간 거칠다.
- 설명할 때도 지나치게 공손하지 않다.
- `난`, `내가`, `~야`, `~지`, `~라고` 계열을 자연스럽게 사용한다.
- `젠장`, `쳇`, `뭐라고?` 정도는 가능하다.
- 현대적 욕설, 인터넷 밈, 과한 비속어는 금지한다.

예시:

| 원문 | 권장 방향 |
|---|---|
| さぁ相手になってやるぜ！ | 자, 상대해 주지! |
| 錬成であっさり終わらせる！ | 연성으로 단숨에 끝내 주지! |
| このコマンドで錬成だ！ | 이 커맨드로 연성이야! |
| とりあえず見とけ | 일단 보고 있어. |

### 8.2 알폰스 엘릭

- 기본은 부드러운 반말.
- 형에게는 `형`이라고 부른다.
- 타인에게는 상황에 따라 존댓말을 사용할 수 있다.
- 설명할 때는 차분하고 친절하다.
- 과하게 어린아이처럼 만들지 않는다.

예시:

| 원문 | 권장 방향 |
|---|---|
| 兄さん、どうやって戦うの？ | 형, 어떻게 싸우는 거야? |
| 素材や技を作るんだね | 소재나 기술을 만드는 거구나. |
| じゃあこれでいい素材をたくさん作ろうよ、兄さん | 그럼 이걸로 좋은 소재를 많이 만들자, 형. |
| 等価交換だね | 등가교환이네. |

### 8.3 로이 머스탱

- 군인답게 단정하고 냉정한 말투.
- 지나치게 가볍거나 장난스럽게 만들지 않는다.
- 부하에게는 명령형, 동료에게는 절제된 반말/존댓말을 문맥에 맞춰 사용한다.

### 8.4 리자 호크아이

- 절제되고 정중한 군인 말투.
- 감정을 과하게 드러내지 않는다.
- 명령·보고·경고는 간결하게 처리한다.

### 8.5 알렉스 루이 암스트롱

- 과장되고 장중한 말투.
- 다만 밈처럼 과하게 희화화하지 않는다.
- 고정 슬롯/UI 계열에서는 길이를 줄이되, Registry D 대사라면 원문 byte 슬롯에 맞추려고 품격이나 의미를 과도하게 압축하지 않는다.

### 8.6 코니슈 로이스 / 코니

- 초반에는 밝고 상냥하며 정중한 말투.
- 후반에는 절망과 결의가 드러나지만, 원문보다 감정을 과하게 설명하지 않는다.
- 오빠 세라피 로이스와 관련된 정체/진상은 초반 대사에 선반영하지 않는다.

### 8.7 아스턴 마틴스

- 반항적이고 거친 면이 있지만 동료애가 강한 군인.
- 계급은 중령. `마틴스 중령`으로 표기.
- 죄수/탈옥/명령 불복 문맥에서는 다소 거친 반말을 허용하지만, 과한 현대 비속어는 금지한다.

### 8.8 링커 / 세라피 로이스

- 링커일 때는 차갑고 압박감 있는 말투.
- 설명을 과하게 늘리지 않는다.
- 위협·집착·냉소가 있는 대사는 짧고 날카롭게 처리한다.
- 세라피 로이스의 과거/본래 성격이 드러나는 장면은 비극성과 선의를 유지한다.

### 8.9 케이트 람

- 천진난만하지만 고집 세고 자존심 강한 말투.
- 어린아이 취급에 반발하는 성향을 살리되, 지나치게 유치하게 만들지 않는다.

### 8.10 랜디 로버

- 투박하고 단순한 말투.
- 힘은 세지만 상식이 부족한 느낌을 살린다.
- 과도한 비속어와 현대 밈은 금지한다.

---

## 9. 호칭 번역 기준

| 원문 | 권장 | 비고 |
|---|---|---|
| 兄さん | 형 | 알이 에드를 부를 때 고정 |
| 大佐 | 대령 | 인명 뒤에서는 띄어쓰기 |
| 中佐 | 중령 | 인명 뒤에서는 띄어쓰기 |
| 少佐 | 소령 | 인명 뒤에서는 띄어쓰기 |
| 中尉 | 중위 | 인명 뒤에서는 띄어쓰기 |
| 教主 | 교주 | 코넬로 문맥 |
| おじさん | 아저씨 | 친근/구어 문맥 |
| お客さん | 손님 | 상점/NPC 문맥 |
| さん | 문맥에 따라 생략 / 씨 / 님 | 과하게 붙이지 않음 |
| ちゃん | 문맥에 따라 생략 / 이름+야 / 이름+ちゃん 대응 | 어린아이/애칭 문맥에서만 반영 |

---

## 10. 스포일러와 정보량 조절

- 초반부 번역에서 후반부 정체, 동기, 관계를 암시하는 표현을 추가하지 않는다.
- 원문이 모호하면 한국어도 모호함을 유지한다.
- 캐릭터의 감정을 원문보다 더 노골적으로 설명하지 않는다.
- 게임 오리지널 캐릭터 관련 대사는 특히 과번역을 피한다.

예:

- 원문이 `もう迷わない`이면 `이제 망설이지 않아` 정도로 처리한다.
- `내 운명을 받아들이겠어`, `그 사람과의 약속을 위해`처럼 원문에 없는 정보를 넣지 않는다.

---

## 11. 문장부호와 표기 규칙

| 원문/상황 | 기준 |
|---|---|
| `…` | 원문에 있으면 유지 우선. 해당 family 에서 표시가 깨지거나 길이가 부족할 때만 `...` 등으로 대체 |
| `・・・` | 원문 형태 유지 우선. 길이/표시 문제가 확인된 경우에만 `…` 또는 `...` 로 축약. `jp_in_translation` 같은 일본어 잔존 검사에서는 보존 특수기호로 취급하고, 실제 가나/한자 잔존과 구분한다 |
| `！！`, `？？`, `！？` | 원문 기호와 강도를 유지 우선. 길이 부족, 화면 잘림, family 별 기호 제한이 있으면 최소한으로 축약 |
| 인명/고유명사 내부의 `・` | 한국어 번역에서는 띄어쓰기로 바꾼다. 예: `エドワード・エルリック` -> `에드워드 엘릭`, `ソニー・クリエイティブ` -> `소니 크리에이티브` |
| `１・２`, `４・５` 같은 숫자 사이의 `・` | 조건/범위 구분 기호로 보아 유지 우선. 표시 문제가 확인된 경우에만 다른 기호로 바꾼다 |
| `／`, `：`, `①` 같은 특수기호 | 일본어/한자를 제외한 기호는 가능한 한 원문 기호를 그대로 유지 |
| 일본어 낫표 `「」` | 원문에서 강조/명칭 경계 역할이면 유지 우선. 한국어 문장상 불필요하거나 길이가 부족하면 생략 가능 |
| 숫자 | `Registry D`, 코어 UI, 게임 용어에서 기존 반각 숫자는 보존. `Entry8`, 이벤트 연출, 오프닝/세이브/선택지 계열은 전각 숫자 우선. 원문 숫자 의미와 번호 체계는 보존 |
| 영문 약어 | 전각 영문 사용. 예: `ＨＰ`, `ＡＳ`, `Ｒ 버튼` |
| `１／５` | `Registry D`, 코어 UI, 게임 용어에서 기존 `1/5` 는 보존. `Entry8`, 이벤트 연출, 오프닝/세이브/선택지 계열은 `１／５` 같은 전각 형태 우선 |

문장부호를 장식적으로 늘리지 않는다.

비권장:

- 뭐야!!!!
- 설마......
- 진짜냐???

---

## 12. 금지 또는 주의 번역

| 원문 | 금지/주의 | 권장 |
|---|---|---|
| 機械鎧 | 기계갑옷, 기계 갑옷 | 오토메일 |
| 合成獣 | 합성수 | 키메라 |
| 国家錬金術師 | 국가연금술사 | 국가 연금술사 |
| 大衆 | 민중, 사람들 | 대중 |
| 錬成 | 조합, 합성 | 연성 |
| 素材 | 자재 | 소재 |
| コマンド | 명령어 | 커맨드 |
| ダイムラー | 다임러 | 다이무라 |
| ブラッドレイ | 브래들리 | 브래드레이 |

---

## 13. 불확실한 항목 처리

- 원문 의미가 불확실하면 무리하게 멋을 내지 않는다.
- 기술명·소재명은 계열성을 우선하고, 판단이 어려운 경우 직역에 가깝게 둔다.
- 추출 데이터가 깨진 문자열처럼 보이면 번역하지 않고 검수 대상으로 남긴다.
- 확정되지 않은 고유명사는 기존 용어표를 따르되, `notes`가 허용될 경우 검토 필요 표시를 남길 수 있다.
